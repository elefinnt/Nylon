{
  "name": "pr-agent",
  "version": "0.1.0",
  "private": true,
  "description": "A CLI that posts AI code reviews onto GitHub pull requests.",
  "license": "MIT",
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -F agent build",
    "dev": "pnpm -F agent dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}
